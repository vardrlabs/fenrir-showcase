# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*~

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Local build artifacts
*.render.html

# Defence in depth: the private repo's standing audit must never appear here.
# It contains unresolved safety findings and vendor criticism. Curation is by
# hand, so a mistaken copy is the realistic failure mode.
internal_report.md
internal_report.*.md

# Paper drafts live in the private repo only. They carry unsourced markers,
# unverified novelty claims and an adversarial self-review.
docs/papers/
paper.md
novelty_register.md
